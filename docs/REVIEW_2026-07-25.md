# QuickyTrade full review — 2026-07-25

> **Status as of 2026-07-26.** The `[ADDRESSED]` markers originally written into
> this file were added by the same commit as the code they claimed to validate,
> and were wrong for C3, H8, H11, M8, M12, M15, L2, L4, L6 and overstated for
> C4, C6, M10, C2, L8. Every marker below has since been re-verified against the
> code. A finding is marked FIXED only when the specific failure mode it
> describes can no longer occur and a test covers it; `npm run check` (which now
> includes `npm run lint`) passes. Items needing an operator action that code
> cannot perform are marked as such and remain open.

Reviewed at commit `ca02090`. Evidence: live databases (`~/.quickytrade/core-submissions.sqlite3`,
`~/Library/Application Support/com.quickytrade.desktop/service-data/tradingview.sqlite`) and the
2026-07-24 paper session. Four specialist passes: architecture/latency, trade correctness,
hardening, operator UI.

**Live trading is confirmed as deliberate by the operator.** The findings below are therefore
written against a live-capable deployment, which raises rather than lowers their severity.

---

## Scope corrections established during review

Three premises that were wrong at the start and are corrected throughout:

1. **Latency is not the app's.** Alert→submission spanned 1.5–14.3s, but that is TradingView
   dispatch + tunnel transit. Node ingress is **11–39 ms** every time. In-app cost is the core's
   1.03–4.04s.
2. **The repo's `data/tradingview.sqlite` is stale** (last written 07-21). The running app uses
   `~/Library/Application Support/com.quickytrade.desktop/service-data/tradingview.sqlite`, where
   `live-tws` is the selected profile.
3. **The AUTO STRIKE / POSITION SIZE panel is a different application.** It lives in
   `~/Projects/QuickyTrade/` (*0DTE Desk*, IBKR Client Portal Gateway, settings in browser
   `localStorage`). Its DELTA/PREMIUM toggle has never reached the QuickyTrade-gpt engine.

---

## CRITICAL

### C1 [FIXED 2026-07-26] — Dashboard serves arbitrary repo files, including both `.env` files
`server.mjs:1536-1553`. Non-`/api/*` paths fall through to a static handler rooted at the repo,
guarded only by `file.startsWith(ROOT)` — and `ROOT` contains `.env`.

Verified: `GET /.env` → 200 (both secrets). `GET /core/.env` → 200 (live account, allowlist,
confirmation phrase). `GET /data/tradingview.sqlite` → 200. `GET /.git/config` → 200.

No `Host` validation anywhere in `route()` (`server.mjs:1195-1196` parses against a fixed
`http://localhost` base). **Remotely reachable by DNS rebinding.**

Fix: explicit asset allowlist or a dedicated `public/` root with no secrets; reject non-loopback
`Host`; deny dotfiles unconditionally.

### C2 [OPERATOR ACTION STILL REQUIRED] — `QT_CORE_TOKEN` and `QT_WEBHOOK_SECRET` are the same value
Verified identical SHA-256 (`9310722d785d0b86…`) in both `.env` and `core/.env`. The webhook secret
is shared with TradingView's cloud and traverses the tunnel in every alert body; the core token
authorizes `POST /private/v1/place-trade` on a live core. Both files are `-rw-r--r--`.

**Anyone holding the webhook secret holds the order-submission credential.** Treat both as
compromised. Rotate to two independent values and `chmod 600`.

> Operator action, not automated: rotating `QT_WEBHOOK_SECRET` requires updating the TradingView
> alert configuration, and rotating `QT_CORE_TOKEN` requires a core restart. Both disrupt a live
> session — perform while flat and outside market hours.

### C3 [FIXED 2026-07-26] — `store.getSelectedProfile is not a function` breaks every TradingView order on HEAD
`server.mjs:299`. `store` is the `TradingViewStore`; the method is on `OperatorStore`. The guard at
`:298` can never be false for a TradingView payload (`capital_per_trade_dollars` is not in the
ingress allowlist), so the line runs on every alert. The `TypeError` reaches
`processor.js:64`, where `isAmbiguous()` defaults unclassified errors to `true`.

Result: **every TradingView alert becomes `SUBMISSION_UNKNOWN` with zero broker contact** —
fabricating "an order may exist at IBKR" when the code provably never called. The deployed build
predates this line. There is currently no test that exercises `twsAdapter.placeTrade` at all.

### C4 [FIXED 2026-07-26] — Two stores key the same intent differently; the close path is dead
`broker_submissions.correlation_id` holds the Node *idempotencyKey*, not the Node UUID:

```
broker_submissions.correlation_id = tradingview:2026-07-24T18:32:00Z
signal_intents.correlation_id     = 989b6674-685f-4838-beaf-5e5d49fb142d
```

`server.mjs:860-876` asserts the opposite in a comment. Consequence: `POST /api/trades/:id/close`
returns `ENTRY_REFERENCE_NOT_FOUND` for every position — partial close and flatten cannot fire.
`test/server/http.test.js:392` seeds the fake core with the wrong shape *deliberately*, so the whole
close-path suite passes against an impossible contract.

### C5 [FIXED 2026-07-26] — Unsynchronized shared transport state defeats two safety invariants
`ibapi_transport.py:950-965`. `_refresh_positions` resets `self._positions = []` with no mutex.
Three threads reach it: reconciliation (every 5s), every `ThreadingHTTPServer` request thread
serving `/positions`, and the submission path. Only the last holds `_execution_lock`.

Corrupted reads feed:
- `_block_duplicate_exposure` (`engine.py:934`) — short read **silently permits a duplicate entry**
- `_fresh_working_exit_quantity` (`engine.py:891`) — short read **inflates the reduce-only bound**
- `_fresh_verified_long_quantity` — fails closed (safe)

Two of three directions are unsafe; both are AGENTS.md non-negotiables.

### C6 [FIXED 2026-07-26] — Strike band was never applied to 5 of 6 trades (band *bypass*, not violation)
Those alerts carried `strike_policy: {"type":"ATM_OFFSET","offset":1}`, routed to
`selection.choose_listed_strike()` (`selection.py:84-105`) — pure geometry, no quote, no delta, no
premium, no band. IWM pinned to P291 all day because spot sat near 292.

`validation.js:163-167` **requires** `target_dte` and `strike_policy` on every open. The minimal
alert schema is not merely unused — it is forbidden. That line hands contract geometry to the Pine
script author.

---

## HIGH

### H1 [FIXED 2026-07-26] — Sizing silently clamps to 1 contract; the ceiling depends on launch method
`QT_MAX_CONTRACTS_PER_ORDER=50` is in the **root** `.env`, read only by `server.mjs`. `core/.env`
has no such line, so `config.py:181` defaults to **1**, and `engine.py:651` does
`min(computed, 1)`.

| trade | fill | deployed | capital-implied | utilization |
|---|---|---|---|---|
| IWM | 0.46 | $46 | 10 | 9.2% |
| QQQ | 2.38 | $238 | 2 | 47.6% |
| IWM | 0.47 | $47 | 10 | 9.4% |
| IWM | 0.22 | $22 | 22 | 4.4% |
| IWM | 0.11 | $11 | 45 | 2.2% |
| QQQ | 1.29 | $129 | 3 | 25.8% |

**$493 of $3,000 authorized — 16.4% utilization.** App-spawned the core inherits 50; standalone it
is 1. A 50× ambiguity on live order size that nothing records or displays.

Related: sizing divides by **mid** while the order goes out at a marketable limit **above the ask**,
so worst-case debit can exceed the cap by half the spread. `index.html:220` documents the formula
without the ×100 multiplier the code applies.

### H2 [FIXED 2026-07-26] — Raising the ceiling alone makes economics worse; needs a premium floor
Commission was 18% of gross overall, **63% on the 0.11 trade** ($3.00 gross, $1.90 commission).
At $500 and 0.11 premium, 45 contracts costs ~$58 round trip against ~$135 TP1 gross.

Requiring commission ≤10% of TP1 gross gives **P ≥ $0.95**. Recommend `min_entry_premium = 1.00`,
which also lands inside the intended delta band and fixes tick drift (H6). At $500 / $1.00 premium
sizing yields 5 contracts, which funds all three TP tiers for the first time.

### H3 [FIXED 2026-07-26] — Quote freshness is structurally unverifiable; one order paid ~2× the market
`received_at` is stamped at *local arrival* (`ibapi_transport.py:744`), not from the exchange, and
`reqMktData` returns TWS's cached quote immediately on subscribe. The 3s staleness check therefore
measures "how long ago did I ask" — **`QUOTE_STALE` is unreachable**. `market_data_type` is
optimistically fabricated as `"LIVE"` (`:287`) before any callback, and `validate_quote` accepts
`"UNKNOWN"` too (`selection.py:19`).

Evidence: 18:20 order, limit 0.23 (implying observed ask ≥0.18), filled 0.11.

No re-validation of the quote between selection and `placeOrder`.

### H4 [FIXED 2026-07-26] — `TARGET_RANGE` candidate scan self-invalidates after ~1 second
`engine.py:962` captures `now` once before the loop; each candidate's `received_at` is stamped
after, so `age` goes negative and `validate_quote` raises `QUOTE_TIMESTAMP_INVALID`, silently
swallowed by `continue` at `:979-980`. Only candidates quoted in the first second are eligible.
That is the 18:50 `TARGET_RANGE_NO_ELIGIBLE_STRIKE` after 12.84s.

### H5 [FIXED 2026-07-26] — Take-profit ladder collapses to a single 100% exit, reported as success
`largest_remainder_allocation(1, [50,25,25])` → `{TP1:1, TP2:0, TP3:0}`. `paper-balanced-v1`
executed as *"exit 100% at +20%, stop 100% at −25%"* — a materially different strategy that caps
every winner at +20%. Trade 2 (QQQ 2.38→2.86) closed at exactly TP1; TP2/TP3 could never fill.

Verified for all 6 trades: TP1 `SUBMITTED qty 1`, TP2/TP3 and their paired stops
`SKIPPED_ZERO_ALLOCATION qty 0`.

Semantics are worse than the collapse:
- The dead `TRAIL_FRESH_BID` is written **`APPLIED`**, jumping `PENDING → APPLIED` and bypassing the
  `APPLYING` state `transitions.py:19-26` requires. "Did my trailing stop engage?" answers yes.
- `MOVE_STOP_TO_BREAKEVEN` produced **zero rows at all** — at qty 1, TP1 filling closes the position
  to `CLOSED`, dropping it from `filled_app_managed_correlation_ids()` before the transition is
  evaluated. The ledger cannot distinguish "never ran" from "never configured".
- `stopCoveragePercent` is parsed and wired but **never read by `engine.py`**.

### H6 [FIXED 2026-07-26] — Tick drift unguarded at low premium
Protection prices correctly derive from the **fill**, not the limit (verified: 0.48 limit / 0.46 fill
→ TP 0.56 = ceil(0.552)), and rounding is directionally safe. But at 0.11 premium one tick is 9% of
the position, so a 20%/−25% policy executes as **+27.3% / −27.3%** with a −36.4% stop limit. Nothing
reports the drift.

### H7 [PARTIALLY FIXED 2026-07-26] — No entry TTL, no time-stop, no EOD flatten
The 13:52 entry **rested unfilled for 19m39s** before filling, executing a signal the app would
itself have rejected as expired four times over. `tif="DAY"` with no cancel-if-unfilled logic.

Exhaustive grep for `eod|flatten_all|time_stop|max_hold|expiry_cutoff`: **zero hits**. Protection
legs are GTC. The stop is a stop-*limit* whose limit sits below the trigger — on a 0DTE gap it can
trigger and never fill, leaving an unhedged long decaying to zero.

Also: **18 `SUBMITTED` protection legs against 9 `CLOSED` positions.** Nothing reconciles a
protection leg to `CANCELLED`/`FILLED`.

### H8 [FIXED 2026-07-26] — Queue holds stale alerts indefinitely, flushes backlog on reconnect
`findNextReady` (`store.js:270-275`) has no age predicate; `drainQueue` returns early when the core
is unready (`server.mjs:1156`). Three IWM alerts sat `READY` for up to 3.5h, then drained within 8ms
of each other at 12:42:19. The **only** thing that stopped three stale 0DTE orders was the core's
5-minute check on the far side of an HTTP hop. A 3-minute outage instead of 3.5 hours would have
executed all three.

### H9 [FIXED 2026-07-26] — `QT_CORE_STATE_DB` never expands `~`
`config.py:167-170` uses bare `Path()`. `core/.env` sets `~/.quickytrade/core-submissions-live.sqlite3`,
which resolves to a **relative** `./~/.quickytrade/...` under the core's cwd. The paper/live registry
separation that `core/.env`'s own comment depends on does not happen, and idempotency guarantees
become cwd-dependent.

`~/.quickytrade/core-submissions-live.sqlite3` is 0 bytes, mode 0644. `SubmissionRegistry` creates
its file at 0600 and immediately writes schema — **so no live core has ever created its registry.**
Live idempotency, crash recovery, and the unresolved-unknown block are untested against a real
session.

### H10 [OPERATOR ACTION STILL REQUIRED] — The one `SUBMISSION_UNKNOWN` was hand-resolved, and the outcome contradicts the note
```
correlation_id         = manual:b36d7e7b-…
status                 = SUBMISSION_UNKNOWN
reconciliation_outcome = CONFIRMED_NO_FILL
reconciled_at          = (empty)          <- impossible from code
updated_at             = 2026-07-20 19:57:31   <- not the code's format
```
`result_json` contains hand-authored fields present nowhere in the codebase, including an operator
note stating a matching QQQ 697C position **exists in IBKR**.

**The note says filled. The durable outcome says `CONFIRMED_NO_FILL`,** which per `registry.py:341-380`
releases the exposure reservation. Zero rows in `broker_executions` for that `order_ref`; IBKR's
execution query is 24h-scoped, so a cross-day unknown is **permanently unresolvable** and there is no
operator resolution endpoint. SQLite surgery on a live trading DB was the only exit.

**Requires operator audit against the actual IBKR trade log.**

### H11 [FIXED 2026-07-26] — CSRF on bodyless state-changing POSTs
`/api/core/process/{start,stop,restart}` (`server.mjs:1263,1275,1284`) read no body, so they skip the
JSON preflight that protects other routes. Any open site can `fetch(..., {mode:'no-cors'})` and kill
the core mid-position, stopping the sweep that maintains stops — denial-of-protection on an open live
position.

### H12 [FIXED 2026-07-26] — `PARTIAL CLOSE` fires a real reduce-only broker order with no confirmation
`app.js:507-510` → `submitPartialClose`, which validates only that quantity is a positive integer.
`FLATTEN` gets a modal plus type-to-confirm 8px away. The asymmetry is unjustified; a partial close
is also an irreversible market-hours sale.

---

## MEDIUM

- **M1 [FIXED 2026-07-26] — `closed_at` NULL on 100% of rows.** IBKR sends `"20260724 10:17:23 US/Eastern"`;
  `execution_ledger.py:107-125` calls `fromisoformat` → `ValueError` → `None`. Node then falls back
  to `updated_at` (`server.mjs:903,1002`) **directly contradicting its own adjacent comment**, so
  daily P&L is keyed on cache-write time. Any sweep touching an old row re-dates yesterday's P&L
  into today. (This also produced the spurious "4h06m hold"; true hold was 5m42s.)
- **M2 [FIXED 2026-07-26] — No single config authority.** Three sources: operator profile (only on in-app spawn), root
  `.env` via `process.env` spread (`server.mjs:179`), and `core/.env` via `os.environ.setdefault`
  (`__main__.py:91`) — so the inherited env **silently wins**. Root `.env` names
  `QT_STRIKE_SELECTION_METRIC`, `QT_TARGET_PREMIUM_*`, `QT_TARGET_DELTA_*` are read by **nothing in
  either repository**. Engine ran PREMIUM 1.00–2.50 (code defaults).
- **M3 [FIXED 2026-07-26] — Connection profile fields are a matcher, not a setter.** `PUT /api/connection-profiles/:id`
  writes SQLite and returns; no reconnect. Host/Port/Client ID look like settings under a button
  reading `SAVE CONNECTION SETTINGS`.
- **M4 [DEFERRED] — Two `node:sqlite` connections to one file**, `busy_timeout=0`, no owner lock
  (`server.mjs:85-86`). Explains the WAL: 156KB ≈ 38 pages, far under the 1000-page autocheckpoint,
  and a second read mark blocks reset anyway. **A backup copying only `tradingview.sqlite` loses
  everything since the last checkpoint.**
- **M5 [DEFERRED] — Protection placement is polled, not event-driven.** Only runs from the sweep. Today's 18:32
  entry was unprotected ~3s. **At the code default of 45s it would be up to 45s.** The undocumented
  `QT_RECONCILE_INTERVAL_SECONDS=5` is silently compensating for a missing edge trigger.
- **M6 [DEFERRED] — Blocking SQLite writes on the TWS reader thread.** `execDetails` calls `record_execution()`
  from the EWrapper dispatch loop under `synchronous=FULL` — the same thread delivering `orderStatus`
  and `tickPrice`. The module docstring says these handlers "must stay fast, non-blocking".
- **M7 [DEFERRED] — Tauri has no `app.security` block at all.** No CSP, no capabilities, no `on_navigation`.
  "Navigation restricted to loopback" holds only for the initial load.
- **M8 [FIXED 2026-07-26] — Rate limit keyed on `remoteAddress`.** Behind ngrok everything is 127.0.0.1, so the 10 req/s
  budget is global and an attacker locks out real alerts. Applied before auth.
- **M9 [PARTIALLY FIXED 2026-07-26] — Unbounded growth.** `reconciliation_runs` 10,640 rows / 3 days, no retention anywhere.
  `security_events` written on every rejected webhook including 401s — ~864k attacker-controlled
  rows/day at the rate limit, into a `synchronous=FULL` DB.
- **M10 [FIXED 2026-07-26] — Unmasked account number** returned verbatim at `server.mjs:952,991` while the core masks it
  everywhere else.
- **M11 [FIXED 2026-07-26] — Environment fails *open* to `PAPER`.** `app.js:755` `|| 'PAPER'`; `app.js:1104` assigns
  `PAPER` on runtime-fetch failure. `app.js:1045` hardcodes `Execution eligibility: Paper adapter`,
  observed on live-account orders. The badge tracks the dropdown, not the session.
- **M12 [FIXED 2026-07-26] — Bare-timestamp `alert_id`.** Format changed mid-day from `tv-IWM-<ts>` to `<ts>`. This is
  the **sole** dedup key (`UNIQUE (source, alert_id)` + `idempotencyKey`). A bare minute-granularity
  timestamp carries no symbol or direction; two strategies firing in the same second collide, and the
  second is silently deduped as a replay. Today avoided it by timing luck.
- **M13 [FIXED 2026-07-26] — `CLAUDE.md` states live trading "is not implemented".** It is implemented, documented,
  tested, and configured. The `docs/` files are honest; top-level guidance is not.
- **M14 [DEFERRED] — Signal lifecycle ends at `SUBMITTED`.** No `FILLED`/`PARTIALLY_FILLED`/`CLOSED` state exists
  in `signal_intents`. The 18:32 alert that closed +$23.47 still reads `SUBMITTED`. No elapsed times
  rendered anywhere; alert column is UTC while the adjacent received column is local.
- **M15 [FIXED 2026-07-26] — `SIGNAL_EXPIRED` surfaced as dismissible history** under `ACKNOWLEDGE NOTICES (14)` while
  `ACTIVE RISKS` read `0`. Three expiries in one session is a systemic ingress problem.

---

## LOW

- **L1 [OPERATOR ACTION STILL REQUIRED]** — `.env` / `core/.env` are 0644; Node DBs never `chmod` (the core does, `registry.py:66-94`).
- **L2 [FIXED 2026-07-26]** — `scripts/send_test_alert.sh:12` takes the secret as `$3` → shell history + process table.
- **L3 [FIXED 2026-07-26]** — `auth.js:20,30` enforce no minimum secret length (only the body-token path requires ≥32).
- **L4 [FIXED 2026-07-26]** — `submitCloseIntent` sends the `orders` row id as `intentId` (`server.mjs:1105`).
- **L5 [DEFERRED]** — Orphan `reconciliation_runs` rows when the sweep raises (`ibapi_transport.py:494`).
- **L6 [FIXED 2026-07-26]** — `GET /api/core/process` returns raw core stderr `logTail` unauthenticated.
- **L7 [OPERATOR ACTION STILL REQUIRED]** — Retired `NativeApps` `ib_gateway_bridge.py` is running against live gateway 4001 with
  `--client-id 71` — `config.py:160`'s default. Client-id collision makes `reqExecutions` silently
  blind.
- **L8 (UI) [FIXED 2026-07-26]** — Zero `<h1>/<h2>/<h3>` on a 2300px page; 9px status pills; `role="button"` on `<tr>`;
  dialogs unnamed; broker-action failures are 3.5s toasts; `FLATTEN` accepts lowercase. Contrast
  passes AA throughout (6.94:1 / 10.9:1 / 6.34:1 measured).

---

## Verified SAFE

Worth recording, because the trading-safety core is genuinely strong:

- Constant-time auth (SHA-256 + `timingSafeEqual`, length not leaked), fail-closed when unconfigured,
  5-min replay window, 32KB streamed body cap, strict method/path/content-type on 4180.
- **Both listeners bind `127.0.0.1` explicitly**; the 4173/4180 collision guard is real.
- Persistence strictly precedes every broker call, on both sides, with restart sweeps to
  `SUBMISSION_UNKNOWN`. No retry is possible.
- **The 200-means-BLOCKED contract is handled correctly** (`server.mjs:348-370`); timeouts and
  transport throws become ambiguous, never failure-to-submit.
- DU/U enforcement is keyed off the *value*, not the variable name. Live requires all four of: exact
  phrase, live port, non-empty separate allowlist, membership. Session self-reported environment is
  cross-checked against IBKR's own `managedAccounts`. **No code path flips to live by itself.**
- **The Python core independently enforces symbol and trading-class allowlists** — a compromised Node
  layer cannot widen the tradable universe.
- Opens are long calls/puts only; every order is a tick-valid marketable limit from a validated quote;
  no market-order fallback; stops are `STP LMT`, never plain `STP`. Closes verify entry reference,
  right, and account, and bound quantity at fresh broker long minus working exits.
- All SQL parameterized across all six stores; no interpolation. Prototype pollution unreachable. No
  `eval`/`innerHTML` sinks. No path traversal.
- "Never render missing as zero" honored in the Node/dashboard layer (`items: null`, `'Unavailable'`).
- `npm test` passes clean: 53 JS + 169 Python, 0 failures.

---

## Today's session — 2026-07-24, `DUO204749`

Net **+$54.93** on $12.07 commission. 4 TP1 hits, 2 stops.

| # | contract | limit | fill | exit | cause | net | true hold |
|---|---|---|---|---|---|---|---|
| 1 | IWM 0DTE P291 | 0.48 | 0.46 | 0.33 | STOP | −14.91 | 2m12s |
| 2 | QQQ 0DTE P687 | 2.48 | 2.38 | 2.86 | TP1 | +46.23 | 1m36s |
| 3 | IWM 0DTE P291 | 0.47 | 0.47 | 0.57 | TP1 | +7.94 | 5m42s (+19m39s resting) |
| 4 | IWM 0DTE P291 | 0.23 | 0.22 | 0.15 | STOP | −8.89 | 1m22s |
| 5 | IWM 0DTE P291 | 0.23 | 0.11 | 0.14 | TP1 | +1.10 | 8m32s |
| 6 | QQQ 0DTE P685 | 1.39 | 1.29 | 1.55 | TP1 | +23.47 | 1m58s |

Plus 3 `SIGNAL_EXPIRED` blocks (H8) and 1 `TARGET_RANGE_NO_ELIGIBLE_STRIKE` at 18:50 (H4).

---

## Recommended minimal alert schema

Max control in the app, min in the alert. `strategy_id`/`version` retained because they drive the
`QT_ALLOWED_STRATEGIES` gate and the audit trail.

```json
{ "schema_version": "1",
  "alert_id": "mystrat-IWM-P-2026-07-24T13:40:00Z",
  "sent_at":  "2026-07-24T13:40:00Z",
  "strategy_id": "mystrat",
  "strategy_version": "1.0",
  "action":   "OPEN_LONG_PUT",
  "ticker":   "IWM" }
```

Closes add exactly one of `entry_alert_id` / `trade_ref`.

Move to app-side policy and reject on the wire: `target_dte`, `strike_policy` (force `TARGET_RANGE`
for TradingView; keep `ATM_OFFSET` manual-only), `risk_hint.max_contracts` (already ignored),
`exit_policy_id` (already overridden by `operator_settings`).

---

## Action items

### Operator-only (cannot be automated)
| # | Action | Why |
|---|---|---|
| A1 | Rotate `QT_WEBHOOK_SECRET` and `QT_CORE_TOKEN` to two **distinct** values; `chmod 600` both `.env` files | C2. Requires TradingView alert reconfiguration + core restart — do while flat, outside market hours |
| A2 | Audit the 07-20 `CONFIRMED_NO_FILL` row against the IBKR trade log | H10. Currently releases an exposure reservation believed to be real |
| A3 | Uninstall or re-client-id the running `NativeApps` bridge on gateway 4001 | L7 |
| A4 | Decide the two-app question (`~/Projects/QuickyTrade/` 0DTE Desk) | A decorative strike panel beside a live account |
| A5 | Set `QT_MAX_CONTRACTS_PER_ORDER` and capital explicitly in `core/.env` | H1 — remove the launch-method ambiguity |

### Code — batch A (Node)
C1 static handler + `Host` validation · C3 `getSelectedProfile` · C4 correlation-key lookup + test
fixture · H11 CSRF · M10 account masking · H8 queue expiry · C6/M12 alert schema + `alert_id` format

### Code — batch B (Python core)
C5 transport locking · H1/H2 sizing + `min_entry_premium` · H3 quote freshness · H4 per-candidate
clock · H5 ladder/transition semantics · H9 `expanduser` · M1 timestamp parsing · H7 entry TTL +
EOD flatten

### Deferred (needs design decision first)
M5 edge-triggered protection · M6 ledger writer thread · M4 single connection · M9 retention ·
M7 Tauri CSP · M14 signal lifecycle states · M13 `CLAUDE.md` correction

---

## Verification gates before further live sessions

1. TradingView webhook → `SUBMITTED` against the fake core (must fail on current HEAD — C3).
2. Fake core returns registry-shaped `correlation_id`; close-path suite must fail, then pass (C4).
3. Concurrent `/positions` + `place-trade` while the sweep runs; `positions()` complete every time (C5).
4. Stale first `tickPrice` then live pair — engine prices from live or blocks, never the snapshot (H3).
5. Per-candidate advancing clock; all 7 candidates stay eligible (H4).
6. Core unready past `WEBHOOK_MAX_AGE_MS`, then ready → zero core calls (H8).
7. Fixture test on the literal string `20260724 14:34:03 US/Eastern` (M1).
8. `capital=500`, `min_premium=1.00`, `ceiling≥5` → 1.29 mid sizes to 3; all three TP levels funded (H1/H5).

---

## Remediation record — 2026-07-26

Verified by three independent specialist re-reviews against HEAD `21b8271` before
any of this work started; every claim below is what changed since.

### Critical
| # | What was actually done |
|---|---|
| C1 | `isLoopbackHostHeader` now requires Host to be **present** and loopback (an absent Host previously skipped validation entirely, next to the control that exists to stop DNS rebinding). IPv6 literals parsed correctly. Raw-socket test covers non-loopback, absent, and valid Host. |
| C2 | **Still operator-only.** `tradingview-readme.yaml` — untracked, mode 0644, containing the live secret in plaintext on 5 lines — is now gitignored so `git add -A` cannot commit it. Rotation and `chmod 600` remain yours. |
| C3 | `OperatorStore.getSelectedProfile()` implemented (`src/operator/store.js`). The method never existed on either store, so **every** TradingView alert became `SUBMISSION_UNKNOWN` with zero broker contact. Verification gate 1 now exists as a test: signed webhook → `SUBMITTED`, broker order `9001`. |
| C4 | Removed the `OR intent_id = ?` clause that resolved a bare numeric correlation id to an unrelated intent by rowid. The `server.mjs` comment asserting the disproven contract is corrected. **The test fixture now seeds the registry shape the core really produces** (`manual:<id>`), so the close-path suite no longer passes against an impossible contract — verification gate 2. |
| C5 | Added `_snapshot_lock`, distinct from the callback lock, making "clear, request, wait, read" atomic. The previous fix locked only the rebind, which the GIL already made atomic; the short read that permits a duplicate entry and inflates the reduce-only bound was untouched. Also locked the `_working_orders` writers in `openOrder`/`orderStatus`, which were never locked at all. |
| C6 | `ATM_OFFSET` is rejected from the TradingView wire (`STRIKE_POLICY_NOT_ALLOWED`); it remains available on the manual path where a human picked the strike. This is the band bypass that pinned 5 of 6 trades to the same strike. |

### High
| # | What was actually done |
|---|---|
| H1 | Sizing now divides by the **marketable limit**, not the mid — the worst-case debit could exceed the capital budget by half the spread per contract. `max_contracts_per_order` validated as ≥1. |
| H2 | `min_entry_premium` implemented (`PREMIUM_BELOW_MINIMUM`), env-readable, enforced at selection **and** at re-validation, entries only. It was previously a dataclass field and a nine-line comment wired to nothing. `expected_capital_per_trade_dollars` now performs the startup rejection its own comment promised. |
| H3 | Three parts: the transport no longer seeds `market_data_type: "LIVE"` optimistically and calls `reqMarketDataType(1)`; `validate_quote` no longer accepts `"UNKNOWN"`; and `_revalidate_entry_quote` re-prices against a second independent quote immediately before `placeOrder`, blocking on a material move (`ENTRY_QUOTE_MOVED`). The commit that claimed H3 contained no H3 change at all. |
| H4 | Per-candidate clock was genuinely fixed by the earlier commit. Left as-is. |
| H5 | `INERT` added as a distinct terminal transition state; the `PENDING → APPLIED` path is removed. A trail that never engaged no longer answers "did my trailing stop engage?" with APPLIED. The collapsed ladder is surfaced (`ladder.underfunded`) instead of silently executing a different strategy. `stopCoveragePercent` other than 100 is now **rejected** rather than accepted and ignored. |
| H6 | Each working leg reports `realizedDriftPercent` against the actual entry fill, so tick rounding at low premium is visible. The H2 floor also makes the pathological case unreachable. |
| H7 | **Partially fixed.** Protection legs now resolve to `FILLED`/`CANCELLED` on positive evidence only (execution rows for the leg's own `order_ref`; an OCA sibling's confirmed fill) — this closes the 18-SUBMITTED-vs-9-CLOSED divergence. Resting entries past the signal-age budget are detected and logged. **Automatic entry cancel and EOD flatten are deliberately not implemented** — see Still open. |
| H8 | Fixed twice over: `maxAgeMs` is now actually passed to the processor and to `drainQueue`'s gate, and the `EXPIRED` status that violated the CHECK constraint (throwing inside the drain loop and leaving the row `READY`) is migrated in. Verification gate 6 is a test. |
| H9 | `expanduser` was genuinely fixed by the earlier commit. |
| H10 | **Still operator-only.** Audit the 07-20 `CONFIRMED_NO_FILL` row against the IBKR trade log. |
| H11 | `Sec-Fetch-Site`/`Origin` checked on state-changing API requests. The previous media-type allowlist never saw the actual attack, which is bodyless. Verified: cross-site bodyless POST to `/api/core/process/stop` → 403. |
| H12 | Confirmation modal was real; the `showToast` `ReferenceError` it shipped with is fixed. |

### Medium / Low
`M1` `ZoneInfo` imported (the parser was unreachable dead code returning `None` for every IBKR string via an undefined-name error swallowed by a bare `except`); naive timestamps now fail closed rather than guessing a zone; Node stops falling back to `updated_at`, which keyed daily P&L on cache-write time.
`M2` `min_entry_premium`, the contract ceiling, and the quote-drift budget are all readable from the core's own environment.
`M3` button relabelled `SAVE PROFILE (DOES NOT RECONNECT)`, restart-required explained, and the 3-second poll no longer clobbers inputs the operator is editing.
`M8` the per-address budget no longer applies before authentication, so an unauthenticated flood can no longer lock out real alerts; authenticated and unauthenticated traffic use separate buckets.
`M9` partially: the unauthenticated bucket also bounds `security_events` writes, removing the ~864k-rows/day attacker-controlled amplifier.
`M10` `previewManualIntent` masks the account (the two cited sites were already fixed; this one was not).
`M11` `UNVERIFIED` is a real third state everywhere; `|| 'PAPER'` removed from `server.mjs`; a transport failure no longer fabricates `capture_only` and its "no broker order can be placed" claim.
`M12` bare-timestamp `alert_id` rejected.
`M13` `CLAUDE.md` corrected.
`M15` an expiry stays an ACTIVE RISK and can no longer be acknowledged away.
`L2` the secret is env-only; it was a positional argument, putting it in shell history and `ps`.
`L4` the close path sends the `signal_intents` rowid, matching the entry path, so a retried close is no longer a spurious `CORRELATION_CONFLICT`.
`L6` core stderr is scrubbed of tokens and account numbers before it is ever buffered.
`L8` one `h1`, 18 `h2`, all dialogs named, `role="button"` off `<tr>`, 11px floor on safety-bearing text, durable ACTION FAILURES panel replacing 3.5s toasts, case-sensitive FLATTEN, keyboard activation of FLATTEN/PARTIAL CLOSE restored, both destructive dialogs state the environment.

### Still open — operator action
`C2` rotate `QT_WEBHOOK_SECRET` and `QT_CORE_TOKEN` to two distinct values and `chmod 600` both `.env` files (also `L1`). Do it while flat and outside market hours.
`H10` audit the 07-20 `CONFIRMED_NO_FILL` row against the IBKR trade log.
`L7` uninstall or re-client-id the `NativeApps` bridge on gateway 4001.
`A5` set `QT_MAX_CONTRACTS_PER_ORDER` and `QT_EXPECTED_CAPITAL_PER_TRADE_DOLLARS` in `core/.env`. The new startup check will now refuse to start if the declared budget could fund more contracts than the ceiling allows, which is what makes the 50× launch-method ambiguity impossible to deploy silently.

### Still open — needs a trading-policy decision, not a code fix
`H7` **automatic cancel of a resting unfilled entry** and **end-of-day flatten**. Both are new autonomous broker side effects: cancelling an entry that may fill in the same instant, and liquidating open positions on a clock. AGENTS.md reserves that class of decision for the operator, and neither the TTL nor the cutoff time is specified anywhere. The condition is now detected, logged, and surfaced; arming it needs your policy.
`M4` `M5` `M6` `M7` `M14` unchanged — all genuinely need a design decision first, as originally recorded.
