{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // empty' | { read -r f; [ -z \"$f\" ] && exit 0; case \"$f\" in *.mjs|*.js) node --check \"$f\" ;; */core/*.py) python3 -m py_compile \"$f\" ;; esac; } 2>&1",
            "statusMessage": "Syntax-checking edited file..."
          }
        ]
      }
    ]
  }
}
